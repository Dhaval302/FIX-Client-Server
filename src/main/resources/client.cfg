[DEFAULT]
ConnectionType=initiator
SocketConnectHost=127.0.0.1  # Server's address (localhost)
SocketConnectPort=9876  # Server's port (should match the server configuration)
HeartBtInt=30  # Heartbeat interval in seconds
FileStorePath=clientStore  # Directory for storing message history
FileLogPath=clientLog  # Directory for logs
DataDictionary=CustomFIX44.xml

[SESSION]
BeginString=FIX.4.4
SenderCompID=ClientCompID  # Client's identifier
TargetCompID=ServerCompID  # Server's identifier
